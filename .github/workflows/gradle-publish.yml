name: IITP

on:
  push:
    branches: [dev]
  release:
    types: [created]

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_IITP_HOST }}
          port: ${{ secrets.REMOTE_IITP_SSH_PORT }}
          username: ${{ secrets.REMOTE_IITP_SSH_USERNAME }}
          password: ${{ secrets.REMOTE_IITP_SSH_PASSWORD }}
          script: |
            DEPLOY_PORT=8888
            cd /home/iitp
            if [ -d iitp ]; then rm -rf iitp; fi
            git clone https://github.com/kesti-ITBIZ/iitp.git
            cd iitp/frontend
            npm i
            cp ./font-awesome-pro-icons/* ./node_modules/@fortawesome/free-solid-svg-icons
            npm run build
            cd ..
            chmod +x ./gradlew
            ./gradlew bootWar
            if [ `docker ps | grep -c 'tomcat/iitp'` != 0 ]; then
              docker stop iitp
              docker rm iitp
            fi
            if [ `docker images | grep -c 'tomcat/iitp'` != 0 ]; then
              docker rmi tomcat/iitp
            fi
            docker build -t tomcat/iitp .
            docker rmi java:8
            docker run -d --name iitp -p $DEPLOY_PORT:9200 tomcat/iitp
            cd ..
            if [ -d iitp ]; then rm -rf iitp; fi
